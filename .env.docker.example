# Copied to .env.docker for `npm run docker:up`.
# Compose also reads host `.env` for JWT_SECRET and OPENAI_API_KEY.

NODE_ENV=production
LOG_LEVEL=info
OPENAI_MODEL=gpt-4o
MOBILE_ORIGIN=*
APPLE_CLIENT_IDS=com.cbtinsomnia.mobile
GOOGLE_CLIENT_IDS=
